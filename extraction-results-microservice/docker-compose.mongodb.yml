services:
  mongodb:
    image: mongo:7
    container_name: mongodb_results
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: extraction_results
    volumes:
      - mongo_results_data:/data/db

volumes:
  mongo_results_data:

