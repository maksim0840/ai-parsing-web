services:
  mongodb:
    image: mongo:7
    container_name: mongodb_orchestrator
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: tasks
    volumes:
      - mongo_orchestrator_data:/data/db

volumes:
  mongo_orchestrator_data:

