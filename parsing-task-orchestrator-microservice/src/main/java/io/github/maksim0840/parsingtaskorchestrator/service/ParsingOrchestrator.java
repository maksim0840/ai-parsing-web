package io.github.maksim0840.parsingtaskorchestrator.service;

public class ParsingOrchestrator {

    // Отвечает за порядок запросов
    public first(boolean htmlParserRequired, boolean textRecognitionRequired, boolean llmRequestRequired) {
        if (htmlParserRequired) {

        } else if (textRecognitionRequired) {

        } else if (llmRequestRequired) {

        }
        return result;
    }
}
